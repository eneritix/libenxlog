# Options
options:
  default_loglevel: ERROR

# Configure sinks
sink:
  type: stdout

sink:
  type: file
  path: "test_config_parser.log"

# Configure filters
filter:
  a.b.c: DEBUG
  one.two.three: DEBUG
